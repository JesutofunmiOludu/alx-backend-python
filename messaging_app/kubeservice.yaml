apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  type: ClusterIP
  selector:
    app: messaging-app
    version: blue    # Change to "green" when promoting the new version
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
